/** Standard 15puzzle dimension */
export const DEFAULT_SIZE = 4 as const;

/** Minimum allowed width or height */
export const MIN_SIZE = 2 as const;

/** Maximum allowed width or height */
export const MAX_SIZE = 100 as const;

/** Value representing the empty space in the grid */
export const EMPTY_TILE = 0 as const;
